import { Typography } from "@mui/material";

const Settings = () => {
  // console.log("Contact Us Page rendered");
  return (
    <>
      <Typography
        sx={{
          margin: "500px",
        }}
      >
        Settings
      </Typography>
    </>
  );
};

export default Settings;
